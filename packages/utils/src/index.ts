// @moveros/utils — shared utilities (dates, addresses, move-type detection).
// Implemented in Phase 1+. Placeholder export keeps the package type-checkable.

export const UTILS_PACKAGE = "@moveros/utils" as const;
