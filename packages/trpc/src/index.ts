// @moveros/trpc — tRPC v11 routers, context, auth + rate-limit middleware.
// Phase 1 adds: context (user from Supabase JWT), protectedProcedure, and the
// moves router. Placeholder export keeps the package type-checkable.

export const TRPC_PACKAGE = "@moveros/trpc" as const;
